{
  "name": "tfjs-examples-mnist",
  "version": "0.1.0",
  "description": "",
  "main": "index.js",
  "license": "Apache-2.0",
  "private": true,
  "engines": {
    "node": ">=8.9.0"
  },
  "dependencies": {
    "@tensorflow/tfjs": "^1.0.0",
    "@tensorflow/tfjs-vis": "^1.0.3"
  },
  "scripts": {
    "watch": "cross-env NODE_ENV=development parcel index.html --no-hmr --open",
    "build": "cross-env NODE_ENV=production parcel build index.html --no-minify --public-url ./",
    "link-local": "yalc link"
  },
  "devDependencies": {
    "@babel/core": "^7.0.0-0",
    "@babel/plugin-tensor-safe": "../../tensor-safe/extra/javascript/babel-plugin-tensor-safe/",
    "@babel/plugin-transform-runtime": "^7.1.0",
    "babel-eslint": "^10.0.1",
    "babel-preset-env": "~1.6.1",
    "clang-format": "~1.2.2",
    "cross-env": "^5.1.6",
    "eslint": "^5.16.0",
    "eslint-plugin-tensor-safe": "../../tensor-safe/extra/javascript/eslint-plugin-tensor-safe/",
    "parcel-bundler": "~1.10.3",
    "yalc": "~1.0.0-pre.22"
  }
}
